{
    "name": "feature",
    "version": "0.0.0",
    "description": "",
    "private": true,
    "scripts": {
        "bootstrap": "npx lerna@3 bootstrap --hoist",
        "prestart": "npm run bootstrap",
        "start": "startup start",
        "prebuild": "npm run bootstrap",
        "build": "startup build",
        "lint": "startup lint",
        "test": "startup test"
    },
    "devDependencies": {
        "@sandboxes/startup": "~8.3.1"
    }
}
